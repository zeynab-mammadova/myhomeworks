package az.company.classes.lesson14;

import java.util.ArrayList;
import java.util.List;

public class StringCheckRotated {
  public boolean check(String origin, String rotated) {
    List<String> orig = new ArrayList<>();
    for (int i = 0; i < origin.length(); i++) {
    StringBuilder stringBuilder=new  StringBuilder(1);
      stringBuilder.append("Hello World").substring(1);
      stringBuilder.substring(0);

    }
  return true;
  }

}
